@model IEnumerable<Capstone1.Models.FamilyLink>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsApproved)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RelationshipType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserProfile.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserProfile1.UserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsApproved)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RelationshipType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserProfile.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserProfile1.UserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
